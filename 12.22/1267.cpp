#include <bits/stdc++.h>
using namespace std;

int main(void)
{
	ios::sync_with_stdio(0);
	cin.tie(0);
	
	int n, t[3];
	int y, m;
	
	cin >> n;
	for(int i=0; i < 3; i++)
		cin >> t[i];
		
	for(int i=0; i<3; i++)
	{
		if(t[i] 
	}
}
